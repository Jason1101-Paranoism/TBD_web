---
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import GoogleAnalytics from '../components/GoogleAnalytics.astro';
import { site } from '../config/site';

interface Props {
  title: string;
  description: string;
  bodyClass?: string;
  canonicalUrl?: string;
  ogType?: string;
  seoTitle?: string;
  schema?: object;
}

const { title, description, bodyClass = '', canonicalUrl, ogType = 'website', seoTitle, schema } = Astro.props;
const canonical = canonicalUrl ?? `${site.siteUrl}${Astro.url.pathname}`;
const pageTitle = seoTitle ?? title;

const siteSchema = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "EducationalOrganization",
      "name": "TBD Studio",
      "url": site.siteUrl,
      "email": site.email,
      "description": "TBD Studio 協助學生進行志向探索、申請策略、備審面試與歷程規劃的專業升學顧問。",
      "areaServed": "TW",
      "availableLanguage": "zh-TW"
    },
    {
      "@type": "WebSite",
      "name": "TBD Studio",
      "url": site.siteUrl,
      "potentialAction": {
        "@type": "SearchAction",
        "target": {
          "@type": "EntryPoint",
          "urlTemplate": `${site.siteUrl}/pages/resources.html?q={search_term_string}`
        },
        "query-input": "required name=search_term_string"
      }
    }
  ]
};
---

<!doctype html>
<html lang="zh-TW">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{pageTitle}</title>
  <meta name="description" content={description}>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&family=Noto+Sans+TC:wght@400;500;700;800&family=EB+Garamond:wght@400;600&display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com" is:inline></script>
  <script is:inline>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            'tbd-light': '#D0CEDB',
            'tbd-mid': '#767995',
            'tbd-accent': '#1A5D94',
            'tbd-dark': '#142143',
            'tbd-yellow': '#FAB748',
          },
          fontFamily: {
            sans: ['Inter', 'Noto Sans TC', 'sans-serif'],
            serif: ['EB Garamond', 'serif'],
          },
          boxShadow: {
            'soft': '0 10px 30px rgba(20, 33, 67, 0.08)',
          }
        }
      }
    }
  </script>
  <link rel="stylesheet" href="/css/style.css">
  <GoogleAnalytics />
  <meta name="google-site-verification" content="google524eb5b6fa89d030">
  <link rel="canonical" href={canonical}>
  <meta property="og:type" content={ogType}>
  <meta property="og:locale" content="zh_TW">
  <meta property="og:site_name" content="TBD Studio">
  <meta property="og:title" content={pageTitle}>
  <meta property="og:description" content={description}>
  <meta property="og:image" content={site.ogImage}>
  <meta property="og:url" content={canonical}>
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content={pageTitle}>
  <meta name="twitter:description" content={description}>
  <meta name="twitter:image" content={site.ogImage}>
  <script type="application/ld+json" is:inline set:html={JSON.stringify(siteSchema)} />
  {schema && <script type="application/ld+json" is:inline set:html={JSON.stringify(schema)} />}
</head>
<body class={bodyClass}>
  <Nav />
  <slot />
  <Footer />
  <script src="/js/main.js" is:inline></script>
</body>
</html>
