# 模板規格稽核（2026-08-23）

> 執行方式：`node scripts/audit-templates.mjs`（加 `--strict` 時有缺漏就 exit 1）。
> 規格來源：YC 的 Batch1／Batch2 審閱報告，加上本次針對三個從未被審過的類型所做的補審。

---

## 一、最重要的發現：兩批審閱的優化成果，一份都沒有落地

repo 裡八群 `proposal-framework` 的行數是 **12／27／41／44／48／50／59／73**，
和 Batch2 審閱報告記載的「原始行數」**完全一致**。也就是說正式站上讓學生下載的，
仍然是被審出問題的 v1。比較表類同樣停在 4 欄（設計比較表仍是 22 行 4 欄，
審閱報告記的優化版是 69 行 6 欄）。

| 批次 | 優化版位置 | 進 repo 了嗎 |
|---|---|---|
| Batch1 v2（比較表 × 7 群 ＋ 通用甘特圖） | Drive `01_優化版/Batch1_v2_20260818/` | ❌ 0 份 |
| Batch1 v2 補審（設計比較表 22→69 行） | Drive `01_優化版/Batch1_v2_20260823/` | ❌ 0 份 |
| Batch2 v2（計畫書架構 × 8 群） | Drive `01_優化版/Batch2_v2_20260823/` | ❌ 0 份 |

**落地時的注意事項**：YC 只優化了 `.csv`，但 repo 的每份模板是 `.md` ＋ `.csv` 成對，
指南頁與下載頁兩種格式都提供。只換 CSV 會讓同一份模板的兩個下載檔內容不同——
**必須成對更新**，不能只覆蓋 CSV。

---

## 二、三個從未被審過的類型（本次補審）

Batch1 審的是比較表、Batch2 審的是計畫書架構，五類模板只涵蓋了兩類。
`contact-email`／`portfolio-checklist`／`oral-checklist` **從未進過任何一批審閱**，
目前共 27 份（9 群 × 3 類）。

規格是從各學群最完整的樣本反推的，已寫進 `scripts/audit-templates.mjs` 的 B3 區塊。

### 缺漏率

**contact-email（9 份）**

| 元件 | 缺 |
|---|---|
| 好壞寫法對照 | 8/9 |
| 使用說明第 2 行 | 6/9 |
| 先判斷該不該寄 | 3/9 |
| 信件結構分段 | 3/9 |
| 寄信時機 | 2/9 |
| 欄數非 6 | 8/9 |

**portfolio-checklist（9 份）**

| 元件 | 缺 |
|---|---|
| 使用說明第 2 行 | 8/9 |
| 常見失分點專區 | 8/9 |
| 送出前檢核 | 8/9 |
| 逐段該寫出什麼 | 7/9 |
| 經歷／素材盤點 | 4/9 |
| 欄數非 6 | 7/9 |

**oral-checklist（9 份）**

| 元件 | 缺 |
|---|---|
| 常見失分點專區 | 9/9（全缺）|
| 使用說明第 2 行 | 6/9 |
| 被質疑時的句型 | 5/9 |
| 題庫分類 | 3/9 |
| 答題框架 | 3/9 |
| 欄數非 6 | 8/9 |

### 兩個異常值

- `grad-law-portfolio-checklist.csv` 是 **7 欄**，全站唯一超出 6 欄的檔案。
- `grad-engineering-*` 四份都在 13–19 行之間，是各類型最薄的——和 Batch2 審閱
  發現「理工 proposal-framework 只有 12 行」是同一個習慣，不是單一類型的問題。

---

## 三、農生環境（2026-08-23 Week 9 新建）

五份模板是照 Batch2 第 06 節共同規格新建的，稽核結果：

| 類型 | 結果 |
|---|---|
| lab-compare | ✓ |
| proposal-framework | ✓ |
| contact-email | ✓ |
| portfolio-checklist | 缺「逐段該寫出什麼」 |
| oral-checklist | 缺「常見失分點專區」 |

後兩項是自評時就看得到的缺口，留著待補；也還沒經過 YC 過審。

---

## 四、為什麼把規格寫成腳本

審閱報告是一次性的產物，規格只存在報告裡。Batch1／Batch2 審完之後優化版停在 Drive、
repo 仍是 v1，中間沒有任何東西會提醒這件事——這就是為什麼要有 `audit-templates.mjs`。

它同時實作審閱報告第 E 節那條規則：**每次有新學群上線，要回頭檢查已結批的類型有沒有漏網**。
設計傳播 2026-08-21 上線時漏了 1 份比較表（8/23 才補審）；農生環境 8/23 上線時，
若沒有這支腳本會一次漏 5 份。

目前刻意**不接進 `npm run verify`**：現況 41/45 份有缺漏，接上去等於讓閘門永遠是紅的。
等 v2 落地、B3 三類補齊之後，再把 `--strict` 接進 verify 當回歸閘門。
